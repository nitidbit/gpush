pre_run:
  - shell: echo "Pre-run command in spec/directory_with_config/gpushrc.yml"

parallel_run:
  - name: parallel_run_1
    shell: echo "Parallel run 1 in spec/directory_with_config/gppushrc.yml"
